---
variables:
  certificate_tar_file:
    help: Path to the certificate tar file generated by 'foremanctl certificate-bundle'.
    type: AbsolutePath
    parameter: --certs-tar-file
    persist: false
