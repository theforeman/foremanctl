---
- name: Setup hammer on the quadlet machine
  hosts:
    - quadlet
  become: true
  vars_files:
    - "../../vars/defaults.yaml"
    - "../../vars/{{ certificate_source }}_certificates.yaml"
  vars:
    hammer_ca_certificate: "{{ server_ca_certificate }}"
  roles:
    - hammer
