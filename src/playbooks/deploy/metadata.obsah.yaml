---
help: |
  Install

variables:
  external_authentication:
    help: External authentication method to use
    choices:
      - ipa
      - ipa_with_api
  external_authentication_pam_service:
    help: Name of the PAM service to use for IPA authentication

include:
  - _flavor_features
  - _flavors/katello
  - _certificates_proxy # Temporary until we dynamically include correct _flavors/*
