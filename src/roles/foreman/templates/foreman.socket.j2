[Unit]
Description=Foreman socket

[Socket]
ListenStream={{ foreman_listen_stream }}
SocketUser={{ httpd_user }}
SocketMode=0600

NoDelay=true
ReusePort=true
# Default on EL10 (v254+), needed explicitly for EL9 (v252)
Backlog=4294967295

[Install]
WantedBy=sockets.target
