---
- name: Deploy iop-inventory-frontend image unit
  ansible.builtin.include_role:
    name: images
    tasks_from: deploy_image.yaml
  vars:
    images_definition:
      name: iop-inventory-frontend
      image: "{{ iop_inventory_frontend_container_image }}:{{ iop_inventory_frontend_container_tag }}"
