#!/bin/bash

USER=$(whoami)

if ! grep -q "^$USER:" /etc/subuid; then
  echo "FAIL: User $USER is missing an entry in /etc/subuid"
  exit 2
fi

if ! grep -q "^$USER:" /etc/subgid; then
  echo "FAIL: User $USER is missing an entry in /etc/subgid"
  exit 2
fi

echo "Subuid/Subgid check: OK"
exit 0
