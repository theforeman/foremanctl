---
- name: Remove legacy iop-advisor image unit and drop-in directory
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ images_quadlet_dir }}/iop-advisor.image"
    - "{{ images_quadlet_dir }}/iop-advisor.image.d"
