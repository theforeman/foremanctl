---
- name: Deploy iop-ingress image unit
  ansible.builtin.include_role:
    name: images
    tasks_from: deploy_image.yaml
  vars:
    images_definition:
      name: iop-ingress
      image: "{{ iop_ingress_container_image }}:{{ iop_ingress_container_tag }}"
