---
- name: Deploy iop-gateway image unit
  ansible.builtin.include_role:
    name: images
    tasks_from: deploy_image.yaml
  vars:
    images_definition:
      name: iop-gateway
      image: "{{ iop_gateway_container_image }}:{{ iop_gateway_container_tag }}"
