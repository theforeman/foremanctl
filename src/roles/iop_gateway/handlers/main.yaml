---
- name: Check if gateway service exists
  ansible.builtin.systemd:
    name: iop-core-gateway
  register: gateway_service_status
  failed_when: false
  listen: restart gateway

- name: Restart gateway service if it exists
  ansible.builtin.systemd:
    name: iop-core-gateway
    state: restarted
  when: gateway_service_status.status is defined and gateway_service_status.status.LoadState != "not-found"
  listen: restart gateway
