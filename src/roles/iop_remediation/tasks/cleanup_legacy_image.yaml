---
- name: Remove legacy iop-remediation image unit and drop-in directory
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ images_quadlet_dir }}/iop-remediation.image"
    - "{{ images_quadlet_dir }}/iop-remediation.image.d"
