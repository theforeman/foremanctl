---
- name: Execute tests
  gather_facts: false
  hosts:
    - localhost
  tasks:
    - name: Run pytest
      ansible.builtin.command:
        cmd: "python -m pytest --durations=10 -vv {{ pytest_args | default() }}"
      args:
        chdir: "{{ inventory_dir }}/../"
      changed_when: false
