---
# Should be flagged (static secret in non-role vars file)
some_database_password: CHANGEME
# Should NOT be flagged (Jinja expression)
some_other_password: "{{ generated_password }}"
# Should NOT be flagged (not a secret name)
some_database_name: mydb
